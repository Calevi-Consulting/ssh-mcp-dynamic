## Validation Report: npm publish preparation (@calevi/ssh-mcp-dynamic: metadata, provenance workflow, docs)
**Date**: 2026-09-06 16:00
**Commit**: pre-commit (branch chore/npm-publish)
**Spec**: specs/003-npm-publish.md
**Status**: PASSED (AC5 verified by the PR's CI run before merge)

### Phase 3: Tests
- Test suite: `npm test`
- Results: 24 passing, 0 failing (no runtime code changed)
- `npm publish --dry-run`: 10 files, dist/ + LICENSE + README.md + package.json only
- Status: PASSED

### Phase 4: Code Quality
- No source changes. Workflow is 45 lines, single job; README edits replace the GitHub install path with the npm one and add a Releasing section
- Status: PASSED

### Phase 5: Security Review (via /ralph-security-review)
- Verdict: CONCERNS (advisory)
- Quoted summary:
  - Phase A: npm audit: 0 vulnerabilities (lockfile changed only in the root package version field; no dependency changes)
  - Phase B (AI-assisted, best-effort, not compliance evidence): publish workflow requests `id-token: write`, required for OIDC trusted publishing and scoped to that job; `contents: read` otherwise. Trigger is `release: published`, which only repository collaborators with write access can fire. Tag/version check prevents publishing a mismatched version. No secrets or tokens introduced. Verdict: clean
  - Phase C: inline AI scan: 0 findings
  - Phase D (advisory): `.github/workflows/publish.yml` added (third-party actions actions/checkout@v4, actions/setup-node@v4 pinned to major tags; `npm install -g npm@latest` pulls the latest npm at run time); `package.json` and `package-lock.json` touched (metadata and version only)
- Status: PASSED (CONCERNS acknowledged)

### Phase 5.5: Release Safety
- Change type: Packaging / CI / docs. No runtime code change
- Rollback plan: revert the PR. For npm, `npm deprecate` after the 72-hour unpublish window
- Status: PASSED

### Overall
- All gates passed: YES (AC5 pending the PR CI run, recorded before merge)
- Notes: first npm publish of 1.1.1 is a manual maintainer action after merge and tag; subsequent releases publish via the workflow once the trusted publisher is configured on npmjs.com.
